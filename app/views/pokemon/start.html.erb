<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>

  <body>
    <h1> You found a pokemon!!</h1>

    HP: <%= session[:pokemon]["hp"] %> <br>
    Name: <%= session[:pokemon]["name"] %> <br>
    <img src="<%= session[:pokemon]["image"] %>">


    <%= button_to "Hit", "/hit", method: :post %>
    <%= button_to "Catch", "/catch", method: :post %>


    <h4>Your Pokemon Collection!</h4>
    <% session[:caught_pokemon].each do |image| %>
    <img src="<%= image %>">
    <% end %>

    <% session[:actions].reverse.each do |action| %>
      <%= action %><br><br>
    <% end %>
  </body>
</html>
